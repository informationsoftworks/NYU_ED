CREATE TABLE recode (
    startswith integer,
    original varchar(5),
    target varchar(5)
);


CREATE TABLE special (
    dx varchar(5),
    category varchar(10)
);


CREATE TABLE xacs (
    dx varchar(5),
    xed1 numeric,
    xed2 numeric,
    xed3 numeric,
    xed4 numeric,
    cases numeric
);

